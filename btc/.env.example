# Bitcoin Monitoring Environment Configuration

# Grafana Configuration
GRAFANA_PASSWORD=admin
GRAFANA_ROOT_URL=http://localhost:3003

# Bitcoin RPC Credentials
BTC_RPC_USER=rpcuser
BTC_RPC_PASSWORD=rpcpassword

# SSL Configuration (optional)
SSL_ENABLED=n
SSL_PROVIDER=none
SSL_EMAIL=
DOMAIN=localhost

# Cloudflare API (if using Cloudflare SSL)
CF_API_TOKEN=
CF_ZONE_ID=
