# Tron Monitoring Environment Configuration

# Grafana Configuration
GRAFANA_PASSWORD=admin
GRAFANA_ROOT_URL=http://localhost:3004

# SSL Configuration (optional)
SSL_ENABLED=n
SSL_PROVIDER=none
SSL_EMAIL=
DOMAIN=localhost

# Cloudflare API (if using Cloudflare SSL)
CF_API_TOKEN=
CF_ZONE_ID=
